import { getAuthProvider, initializeAuthProvider } from '@/lib/providers/auth';
import { getEmailProvider, initializeEmailProvider } from '@/lib/providers/email';

export { getAuthProvider, initializeAuthProvider, getEmailProvider, initializeEmailProvider };
